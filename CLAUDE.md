# Soyorin browser

This project use pyrefly and uv.

## test

Use pytest. But don't make `__main()__`. Make test class for each exercise I give to you.

```bash
uv run pytest <test file path> -v
```

## run

```
uv run main.py <uri>
```